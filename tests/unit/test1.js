
describe('initial test', function () {
    //var _ = require('underscore');

    beforeEach(function () {

    });

    it('makes sure tests are working', function () {
        //expect(_.clone({'test': 1})).toEqual({'test': 1});
    });
});